# TODOS

静态模块（现在需要依赖内存申请，考虑到这个内核不会很大，可以降级到静态模块+取消版本控制）

如何靠这种方式控制运行时内容？引入多段初始化：

- 启动，加载COREMOD，这些模块包含了操作系统的核心功能，比如：
  - 中断和其设备
  - 内存管理系统
  - 调度
  - etc
- 第二阶段，加载SYSMOD，这些模块可以不声明地依赖COREMOD
- 第三阶段，真正的初始化，利用COREMOD的功能进行系统初始化
